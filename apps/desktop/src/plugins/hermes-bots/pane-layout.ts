// This value is consumed by the actual Bots registration and paired with the
// core Sessions width in the pane-renderer regression test. Keep the plugin
// boundary local: plugins may import only their own modules and the public SDK.
export const HERMES_BOTS_PANE_WIDTH = '190px'
